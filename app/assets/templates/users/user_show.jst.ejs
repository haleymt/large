<h1><%= user.escape('email') %></h1>
<h3><%= user.escape('description') %></h3>

<ul><% stories.forEach( function (story) { %>
  <a href="#/stories/<%= story.id %>">
    <li><%= story.escape('title') %><br>
      <%= story.escape('subtitle') %>
    </li>
  </a>
  <% }) %>
</ul>
